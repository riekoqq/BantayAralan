__pycache__/
*.pyc
*.db
.DS_Store

anti-ai-slop/
.claude/scheduled_tasks.lock

# standalone deliverable documents (proposal, prototype paper, capstone
# master doc, feasibility study) — kept locally, not tracked in git
documents/

# root-level dataset photos — kept locally only, same as detection/dataset/
dataset/

# detection/ dataset tracking — never commit raw/labeled images, exports, or weights
detection/dataset/images/
detection/dataset/exports/
detection/dataset/roboflow_export/
detection/runs/
*.pt
*.onnx
